<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Create A Landmark!</h1> <!-- check for empty name!-->
    <form action="/landmarks" method="POST">
      <label for="">Name:</label>
      <input type="text" placeholder="Landmark Name" id=landmark_name name="landmark[name]"><br>

      <label for="">Year Completed:</label>
      <input type="text" placeholder="ex: 1900" id=landmark_year_completed name="landmark[year_completed]"><br>
      <label>Select Figure: </label>
      <select name="landmark[figure]">
      <% @figures.each do |figure| %>
        <option value="<%= figure.id %>"><%= figure.name %></option>
      <% end %>
      </select>
      <br>
      <input type="submit" value="Create New Landmark">
    </form>
  </body>
</html>
