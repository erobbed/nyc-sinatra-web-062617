<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body
    <h1> <%= @figure.name %> </h1>
    <label>Titles Held:</label>
      <% @figure.titles.each do |title| %>
      <li> <%= title.name %> </li>
      <% end %>
      <br>
      <label>Landmarks:</label>
        <% @figure.landmarks.each do |landmark| %>
        <li> <%= landmark.name %> </li>
        <% end %>
  </body>
</html>
